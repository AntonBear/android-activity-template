Пул строк (String Pool)
В Java существует String Pool — особая область памяти, где хранятся все строковые литералы.

Если String был бы изменяемым, то изменение одной строки в пуле изменило бы все переменные, которые на неё ссылаются.

Неизменяемость позволяет безопасно разделять строки между разными частями программы, экономя память.